
/// Entry point to all API services for the application.
class {{topLevelClassName}} extends base.Root {
{{#resources}}
  {{type}} _{{name}};
  {{type}} get {{name}} => _{{name}};
{{/resources}}

  final base.RequestHandler requestHandler;
  final String servicePath;

  {{topLevelClassName}}(this.requestHandler, {this.servicePath: "{{servicePath}}"}) {
{{#resources}}
    this._{{name}} = new {{type}}(this);
{{/resources}}
  }

  Stream send(base.Request request) => requestHandler.handle(request);
}
